git init
git remote add origin https://github.com/dtthom09/Repository.git
git remote -v //confirm remote is correct
git add . //to add all files, if only certain files are needed then select accordingly
git commit -m "commit message"
git push origin master
